spring:
  datasource:
    url: jdbc:postgresql://localhost:5134/shukai_chat
    username: shukai_chat
    password: shukai_chat

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8861/eureka
